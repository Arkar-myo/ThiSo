export interface Song {
    id: number
    title: string
    artist: string
  }
  
  export interface User {
    id: number
    username: string
    email: string
  }
  
  // Add more shared types as needed